# Roadmap

- [x] Server-rendered per-session OG/Twitter HTML for result pages (og-result edge function, crawler UA detection + 302 for humans)
- [ ] Platform preview panel in share dialog (final post text, char counts, exact OG image URL per platform)
- [ ] Migrate project to TanStack Start (`/skill:migrate-to-tanstack`) — gives true SSR at `/result/<id>` so pretty URLs preview natively

- [ ] Confirm panel grades + verdict on a full real-panelist debate (resume a saved session; live runs take ~15 min of AI streaming)
- Clarify panelist database legal framing: these must be fictional/composite profiles or used with consent; disclaim liability; avoid suggesting users impersonate real investors.
